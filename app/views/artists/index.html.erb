<h1>Artists</h1>

<% @artists.each do |artist| %>
  <p><%= link_to artist.name, artist_path(artist) %></p>
    <p><% artist.songs.each do |song| %>
      <%= song.name %> </p>
    <% end %>

<% end %>
